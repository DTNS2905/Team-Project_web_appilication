<!-- Modal -->
<div class="modal fade" id="modalUpdate<%= index %>" tabindex="-1" aria-labelledby="modalUpdate<%= index %>" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content" style="text-align: left;">
            <div class="modal-header">
                <h1 class="modal-title fs-5 " id="exampleModalLabel">Update Publisher</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form method="POST" action="/admin/publisher/<%= publisher.data[index]._id %>" >
                    <div class="row">
                        <div class="col-lg-12 col-md-12 col-sm-12">
                            <label for="title" class="form-label">Fullname</label>
                            <input type="text" class="form-control" id="title" placeholder="Enter Fullname" name="fullname"
                                required value="<%= publisher.data[index].fullname %>">
                        </div>
                        <div class="col-lg-12 col-md-12 col-sm-12">
                            <label for="description" class="form-label">Country</label>
                            <input type="text" class="form-control" id="title" placeholder="Enter Country" name="country"
                                required value="<%= publisher.data[index].country %>">
                        </div>
                    </div>
                    <div class="modal-footer justify-content-center">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-primary">Save changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
